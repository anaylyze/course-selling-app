{
  "name": "week8-course-selling-app",
  "version": "1.0.0",
  "description": "- Initialize a new Node.js project\r     - Add Express, jswebtoken to it as a dependency\r     - Create index.js\r     - Add route skeleton for user login, signup, purchase a course, see course \r     - Add middleware for user and admin auth\r     - Add a database(mongodb), use dotenv to store the database connection string\r     - Define the schema for User, Admin, Course, Purchase\r     - Complete the routes for user login, signup, purchase a course, see course(Extra points - Use Express routing to better structure your routes)\r     - Create the frontend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.2"
  }
}
